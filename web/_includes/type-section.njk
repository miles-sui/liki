<template x-if="{{ condition }}">
  <div class="card bg-base-100 shadow-sm mb-8">
    <div class="card-body">
      <h2 class="card-title text-xl mb-3" x-text="$store.locale.t('{{ titleKey }}')"></h2>
      <div class="prose prose-base max-w-none text-base-content/80 leading-relaxed whitespace-pre-line" x-text="{{ binding }}"></div>
    </div>
  </div>
</template>
