{
  "name": "compute_xiaoxian",
  "description": "计算小限（命理小限流年）。返回每年干支。当用户询问小限或需要从另一个角度分析年运时调用。",
  "parameters": {
    "type": "object",
    "properties": {
      "gender": {"type": "string", "enum": ["male", "female"]},
      "count": {"type": "integer", "description": "需要计算的年数，默认 12，最大 120"}
    },
    "required": ["gender"]
  }
}
