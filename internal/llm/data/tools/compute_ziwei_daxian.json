{
  "name": "compute_ziwei_daxian",
  "description": "计算紫微斗数大限（十年大运）。返回每十年的大限宫位和四化。需要先通过 compute_ziwei 获取命盘。当用户询问紫微大限时调用。",
  "parameters": {
    "type": "object",
    "properties": {
      "chart": {
        "type": "object",
        "description": "紫微命盘数据，直接传入 compute_ziwei 返回结果中的 data 对象。包含全部十二宫和星曜信息。"
      }
    },
    "required": ["chart"]
  }
}
